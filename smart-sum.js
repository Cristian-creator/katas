// Description:
// write a function named smartSum that receives as parameter an array
// and returns the sum of all elements.
// note: the array can contains nested arrays, but the elements have
// always type number

const smartSum = (arr) => {

};

console.log(typeof [1, 2, 3]);

// output: 18
console.log(smartSum([1, 2, 3, [4, 5, [1], 2]]));